import 'package:flutter/material.dart';

class MMenuIconButton extends StatelessWidget {
  const MMenuIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
